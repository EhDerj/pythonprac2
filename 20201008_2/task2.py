import fractions

newf = lambda lst: fractions.Fraction(lst.pop(0))
newi = lambda lst: int(lst.pop(0))

def fnl(i, ls):
    def f(x):
        res = 0
        while i >= 0

def polinom_check(main):
    main = main.split(', ')
    s = newf(main)
    w = newf(main)
    for i in range(int(main.pop(0))):
